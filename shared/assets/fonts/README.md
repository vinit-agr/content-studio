# shared/assets/fonts/

Local-only `fonts` assets. **Contents of this directory are gitignored**
(only `.gitkeep` and this `README.md` are committed).

Put source `fonts` files here when a project references them. They stay on
your machine; do not commit binaries to the repo.
